/**
 * External dependencies
 */

import { expect } from 'chai';

describe( 'dummy', () => {
	describe( 'dummy thing', () => {
		it( 'should test that 1 = 1', () => {
			expect( 1 ).to.eql( 1 );
		} );
	} );
} );
